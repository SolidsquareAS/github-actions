name: Echo file

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: print directory
        run: ls
      - name: print file
        run: cat ./index.html
